<!DOCTYPE html>
<html>
<head>
  <title>LearningLog</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
	<br />
	<div><img src="images/logo-learning-log.png"></div>
	<br />
	<div>
		<%  if user_signed_in? %>
			<%= image_tag current_user.gravatar_url %> 
			<br/>
			<%= current_user.try(:email) =%>
			&nbsp;
			<%= link_to '(Sair)', "/users/sign_out" %>
		<%end%>	
	</div>

 	<%= yield %>

</body>
</html>
